// This file is got from "https://github.com/wesbos/eslint-config-wesbos"
// Sould have been in gitignore as it's vscode setting for, 1) turning off default formatting, 2) and auto-format on save with eslint
// keeping it in git for my own reference.

{
  // These are all my auto-save configs
  "editor.formatOnSave": true,
  // turn it off for JS and JSX, we will do this via eslint
  "[javascript]": {
    "editor.formatOnSave": false
  },
  "[javascriptreact]": {
    "editor.formatOnSave": false
  },
  // show eslint icon at bottom toolbar
  "eslint.alwaysShowStatus": true,
  // tell the ESLint plugin to run on save
  "editor.codeActionsOnSave": {
    "source.fixAll": true
  },
  "search.exclude": {
    "public/api": true,
  }
}
